{
  "name": "spier",
  "private": true,
  "workspaces": ["extension", "server", "shared", "cli"],
  "scripts": {
    "dev:server": "npx tsx --watch server/src/index.ts",
    "dev:ext": "cd extension && npm run dev",
    "start": "npx tsx server/src/index.ts",
    "mcp": "npx tsx server/src/mcp.ts",
    "cli": "npx tsx cli/src/main.ts"
  }
}
